#include <stdio.h>

const int a = 14, b = 44, c = 42, d = 8;

int main(int argc, char **argv) {
  if (argc == 1) {
    fprintf(stderr, "usage: command packetnum");
    return 1;
  }
  printf("Alice => Bob %s packets\n", argv[2]);
  return 0;
}
